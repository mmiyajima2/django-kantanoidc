language: python
python:
    - "3.6"
env:
    - KAOC_CLIENT_ID="id"
    - KAOC_CLIENT_SECRET="secret"
    - KAOC_SERVER="https://accounts.google.com"

install:
    - pip install tox-travis

script:
    - tox
